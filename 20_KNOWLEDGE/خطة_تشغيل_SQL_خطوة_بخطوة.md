# 🗄️ خطة تشغيل قاعدة البيانات (SQL) — خطوة بخطوة
## لشخص لا يعرف شيئاً عن النظام — اتبع بالحرف

---

## قبل أن تبدأ — ماذا تحتاج؟

النظام يستعمل قواعد بيانات على منصّة اسمها **Supabase**:
- **القاعدة الرئيسية (DB1)** — المنتجات · الفواتير · المبيعات · كل شيء أساسي.
- **قاعدة الكفالة (DB3)** — الكفالات والعقود.
- **قاعدة الموقع (DB4)** — محتوى الموقع والوكلاء.

لكل قاعدة، افتح صفحتها على Supabase → **SQL Editor** → وشغّل ملفاتها **بالترتيب المكتوب تحت، واحداً واحداً**.

> **كيف تشغّل ملفاً؟**
> ١. افتح الملف بمحرّر نصوص (Notepad) وانسخ كل محتواه.
> ٢. في Supabase: القائمة اليسرى → **SQL Editor** → **New query**.
> ٣. الصق → اضغط **Run** (أو Ctrl+Enter).
> ٤. انتظر **Success** ثم انتقل للملف التالي.

---

## 🟦 القاعدة الأولى: الرئيسية (DB1)

من مجلد `01_SQL_شغّلها_أولاً/DB1_MAIN/` — بهذا الترتيب بالضبط:

| # | اسم الملف | ماذا يفعل |
|---|---|---|
| 1 | `GMT_00_BASE_TABLES.sql` | ⚠️⚠️ **الأول قبل كل شيء** — يبني الجداول الأساسية (المنتجات · الفواتير · المستخدمون · الأوردرات...) |
| 2 | `GMT_ACCOUNTING_GUARDS.txt` | سجل التدقيق وأقفال الفترات |
| 3 | `GMT_MASTER_SCHEMA_2026-07-13.sql` | إعدادات gmt_settings + أعمدة إضافية + الحماية (RLS) |
| 4 | `GMT_ORDERS_FIXES.txt` | عدّاد أرقام الأوردرات (النسخة الصحيحة) |
| 5 | `GMT_POS_FIXES.txt` | نظام المرتجعات |
| 6 | `GMT_SETTLEMENTS.txt` | التسويات |
| 7 | `GMT_COMM_AWAIT.txt` | العمولات المعلّقة |
| 8 | `GMT_ADMIN_INVOICE.txt` | مراجعة الفواتير للأدمن |
| 9 | `GMT_ORDERS_TG_MSGID.txt` | ربط الأوردرات بتيليغرام |
| 10 | `GMT_OFFERS_TOOL.txt` | أداة العروض |
| 11 | `GMT_OFFERS_UNIFY.txt` | توحيد العروض |
| 12 | `GMT_MINSTOCK.txt` | حدّ المخزون الأدنى |
| 13 | `GMT_STOCKCOUNT.txt` | جرد المخزون |
| 14 | `GMT_BOTS_2026-07-13.sql` | جداول البوتات + المراقب (⚠️ بدونه لا تصلك تقارير الأخطاء) |

> **اختياري:** `GMT_TEST_USER.txt` — ينشئ مستخدم تجريبي. شغّله فقط إن أردت حساب اختبار.

---

## 🟩 القاعدة الثانية: الكفالة (DB3)

من مجلد `01_SQL_شغّلها_أولاً/DB3_WARRANTY/` — بهذا الترتيب:

| # | اسم الملف | ماذا يفعل |
|---|---|---|
| 1 | `GMT_00_BASE_WARRANTY.sql` | ⚠️ **الأول** — يبني جداول الكفالة الأساسية |
| 2 | `GMT_WARRANTY_TRACKING_2026-07-13.sql` | أعمدة التتبّع الإضافية |
| 3 | `GMT_BIRTHDATE.txt` | حقل تاريخ الميلاد |
| 4 | `GMT_CONTRACTS.txt` | العقود |
| 5 | `GMT_CONTRACTS_V2.txt` | تحديث العقود |

---

## 🟪 قاعدة المتجر (DB2)

من مجلد `01_SQL_شغّلها_أولاً/DB2_STORE/`:

| # | اسم الملف | ماذا يفعل |
|---|---|---|
| 1 | `GMT_00_BASE_STORE.sql` | جداول المتجر (المنتجات · التعليقات) |

---

## 🟨 القاعدة الثالثة: الموقع (DB4)

من مجلد `01_SQL_شغّلها_أولاً/DB4_SITE/`:

| # | اسم الملف | ماذا يفعل |
|---|---|---|
| 1 | `GMT_SITE_AGENTS.txt` | محتوى الموقع والوكلاء |

---

## 🔑 خطوة أخيرة: عيّن نفسك «أدمن»

بعد الانتهاء من القاعدة الرئيسية وتسجيل دخولك أول مرة، شغّل هذا في SQL Editor للقاعدة الرئيسية (بدّل `حسابك` باسم المستخدم):

```sql
update gmt_users set role='admin' where username='حسابك';
```

بدونها لن ترى أدوات الأدمن ولا البوتات.

---

## ✅ كيف تتأكد أن كل شيء تمام؟

بعد كل ملف: يجب أن تظهر **Success**. إن ظهر خطأ:
- **«already exists»** → طبيعي (شُغّل سابقاً). تجاهله وأكمل.
- **أي خطأ آخر** (مثل «column does not exist») → **توقّف** وأرسل نصّ الخطأ. لا تكمل.

بعد كل الملفات: افتح النظام كأدمن → زر **🧪 الفاحص الذاتي** (أسفل اليسار) → يجب أن يكون **أخضر**.

---

## 🆘 البدء من الصفر على قاعدة جديدة فارغة

بدل تشغيل ملفات DB1 الـ13 واحداً واحداً، شغّل **ملفاً واحداً جاهزاً** يجمعها بالترتيب الصحيح:

`07_SQL_استعادة_كاملة/①_DB1_الرئيسية_البنية_الكاملة.sql`

(وبالمثل ②③④ للقواعد الأخرى). ملف واحد → كل البنية دفعة واحدة. ثم ارفع نسختك الاحتياطية للبيانات.

---

## 📌 ملخّص بسطر

**الرئيسية:** ACCOUNTING_GUARDS أولاً → MASTER → الباقي بالترتيب → BOTS آخراً.
**الكفالة:** WARRANTY_TRACKING ثم الباقي. **الموقع:** SITE_AGENTS.
**ثم:** عيّن نفسك أدمن → افحص بـ🧪.

> ✅ كل تعارضات SQL المعروفة (SQL-1 · SQL-2 · SQL-3) مُصلحة. إن اتّبعت الترتيب أعلاه لن تظهر.
