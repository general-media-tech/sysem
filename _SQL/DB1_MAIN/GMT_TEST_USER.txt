-- ══════════════════════════════════════════════════════════
-- GMT — زرع مستخدم تجريبي للدخول (القاعدة الرئيسية)
-- تدخل به إلى: نقطة البيع · إنشاء الكفالة · أداة العقود
--   👤 اسم المستخدم: admin      🔑 كلمة المرور: 1234
-- ⚠️ غيّرهما فوراً بعد أول دخول ناجح (أو احذف الصف وأنشئ حساباتك الحقيقية).
-- آمن للتشغيل أكتر من مرة — لا يكرر المستخدم إن وُجد.
-- ══════════════════════════════════════════════════════════

insert into gmt_users (username, display_name, password_hash, branch_key, is_active)
select
  'admin',
  'المدير العام',
  '1234',   -- النظام يقبل النص الصريح أو ترميز base64 — الصريح أسهل للتجربة
  coalesce(
    (select key_name from inv_columns where is_branch = true order by sort_order asc limit 1),
    'main'
  ),        -- يلتقط أول فرع حقيقي من فروعك تلقائياً
  true
where not exists (select 1 from gmt_users where username = 'admin');

-- ══════════════════════════════════════════════════════════
-- استعلامات مفيدة (شغّلها لوحدها لو حبيت تشوف الوضع):
--   select username, display_name, branch_key, is_active from gmt_users;
--   select key_name, display_name from inv_columns where is_branch = true;
-- ملاحظة: قائمة الفروع بشاشة دخول نقطة البيع تأتي من جدول inv_columns
-- (تُدار فروعه من نظام الجرد) — لو ظهرت "لا توجد فروع" أنشئ فرعاً من الجرد أولاً.
-- ══════════════════════════════════════════════════════════
